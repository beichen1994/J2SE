

进程：
	程序运行出现进程
	进程是系统进行资源分配和调用的独立单位。
	每一个进程都有自己的内存空间

多进程：
	多进程可以在一个时间段内执行多个任务
	提高cpu的使用率

	一边玩游戏，一边听音乐不是同时进行
	单cpu在某一个时间点只能做一件事。
	cpu在高效切换让我们觉得同时进行

线程：
	同一个进程执行多个任务，每个任务是一个线程
	即一个进程里可以有多线程，提高程序的cpu使用率，有随机性
	
并行 ：逻辑上同时发生，在某一个时间内同时运行多个程序
并发：物理上同时发生，在某一个时间点同时运行多个程序

Java程序运行原理：
	由java命令启动Java Virtual Machine ，相当于启动一个进程
	该进程创建一个主线程去调用main方法
	
jvm 虚拟机的启动是多线程的，因为垃圾回收线程也要先启动，否则会出现内存溢出
即最低启动了两个线程